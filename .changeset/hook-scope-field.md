---
"@moonshot-ai/agent-core": minor
"@moonshot-ai/kimi-code": minor
---

Add `scope` field to hook definitions for project-scoped hook execution. Hooks with a `scope` regex only fire when the session working directory matches the pattern.

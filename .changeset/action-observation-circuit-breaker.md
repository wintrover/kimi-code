---
"@moonshot-ai/agent-core": minor
"@moonshot-ai/kimi-code": minor
---

Add an `action-observation` detection mode to the execution guardrails circuit breaker, so it trips only when repeated identical tool inputs also produce identical outputs.

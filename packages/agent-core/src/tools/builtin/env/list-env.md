List all environment variables in the agent's environment store, optionally with change history.

This provides visibility into the current state of the environment, which is injected
into every shell command executed by BashTool.
